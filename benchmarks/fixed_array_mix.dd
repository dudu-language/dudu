def fixed_array_mix(n: i32) -> i64:
    values: i32[8]
    for i: i32 in range(8):
        values[i] = i * 3 + 1

    total: i64 = 0
    for i: i32 in range(n):
        index: i32 = i & 7
        total += i64(values[index])
        values[index] += 1

    return total
