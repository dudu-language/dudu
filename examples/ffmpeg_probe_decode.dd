# FFmpeg allocation/cleanup probe.

import c "libavcodec/packet.h"


def main() -> i32:
    packet: *AVPacket = av_packet_alloc()
    if packet == None:
        return 1

    av_packet_free(&packet)
    if packet != None:
        return 2

    return 42
