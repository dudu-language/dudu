def choose(flag: bool) -> Option[i32]:
    if flag:
        return 1
    return None


def main() -> i32:
    match choose(True):
        case _:
            return 0
        case Some(value):
            return value
