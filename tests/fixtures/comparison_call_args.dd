def choose(value: bool) -> i32:
    if value:
        return 20
    return 10


def main() -> i32:
    total: i32 = 0
    total += choose(4 != 5)
    total += choose(4 <= 5)
    total += choose(5 >= 5)
    return total - 18
