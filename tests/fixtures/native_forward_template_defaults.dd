from cpp.path import native_headers/native_compat_stress.hpp


def main() -> i32:
    value = dudu_compat.ForwardDefault()
    omitted = dudu_compat.OmittedDefaultUse()
    if omitted.get() != 7:
        return 1
    return value.get()
