def sum_view(values: array_view[i32]) -> i32:
    total: i32 = 0
    for value: i32 in values:
        total += value
    return total


def main() -> i32:
    values: array[i32] = [4, 10, 20, 12, 6]
    view: array_view[i32] = values[1:4]
    return sum_view(view)
