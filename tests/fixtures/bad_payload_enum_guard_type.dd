enum Message:
    Move:
        x: i32
        y: i32


def main() -> i32:
    msg: Message = Message.Move(x=1, y=2)
    match msg:
        case Message.Move(x, y) if x:
            return x + y
        case Message.Move(x, y):
            return y - x
