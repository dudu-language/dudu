def sum_values[T](values: span[T]) -> T:
    total: T = T()
    for value: T in values:
        total += value
    return total


def main() -> i32:
    values: array[i32] = [10, 20, 12, 7]
    view: span[i32] = values[0:3]
    return sum_values(view)
