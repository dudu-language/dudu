class Vec3:
    x: i32
    y: i32

    @operator("*")
    def mul_vec(self, other: Vec3) -> Vec3:
        return Vec3(self.x * other.x, self.y * other.y)

    @operator("*")
    def mul_scalar(self, value: i32) -> Vec3:
        return Vec3(self.x * value, self.y * value)
