def main() -> i32:
    scores: dict[str, i32] = {
        "bob": 20,
        "ada": 22,
    }
    seen: set[str] = {
        "bob",
        "ada",
    }
    if seen.contains("ada"):
        return scores["bob"] + scores["ada"]
    return 1
