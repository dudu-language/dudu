import c "jpeglib.h" as jpeg


def main() -> i32:
    error: struct jpeg_error_mgr

    returned = jpeg.jpeg_std_error(&error)
    if returned == None:
        return 1

    if jpeg.JPEG_LIB_VERSION <= 0:
        return 2
    if jpeg.JCS_RGB == jpeg.JCS_UNKNOWN:
        return 3

    return 42
