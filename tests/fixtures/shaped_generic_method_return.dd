class Box[T]:
    values: list[T]

    def first(self) -> T:
        return self.values[0]


def needs_i32(value: i32) -> i32:
    return value + 20


def main() -> i32:
    values: list[i32] = []
    values.append(22)
    box: Box[i32][1] = Box[i32](values)
    return needs_i32(box.first())
