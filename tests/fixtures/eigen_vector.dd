from cpp import Eigen/Dense


def main() -> i32:
    a = Eigen.Vector3f(1.0, 2.0, 3.0)
    b = Eigen.Vector3f(4.0, 5.0, 6.0)
    dot = a.dot(b)
    sum = a + b
    if i32(dot) != 32:
        return 1
    if i32(sum.sum()) != 21:
        return 2
    return 42
