class Parser:
    @operator("()")
    def parse_i32(self, value: i32) -> i32:
        return value + 10

    @operator("()")
    def parse_str(self, value: str) -> i32:
        return i32(len(value)) + 20


def main() -> i32:
    parser = Parser()
    return parser(12) + parser("abcdefghij")
