import c "X11/Xlib.h" as x11


def main() -> i32:
    display = x11.XOpenDisplay(None)
    if display == None:
        return 42

    screen = x11.XDefaultScreen(display)
    root = x11.XRootWindow(display, screen)
    x11.XCloseDisplay(display)

    if root == 0:
        return 1
    return 42
