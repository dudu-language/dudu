class Tensor2:
    values: array[i32][4]

    @operator("[]")
    def at(self, index: i32) -> i32:
        return self.values[index]


def main() -> i32:
    values: array[i32] = [10, 20, 12, 1]
    tensor = Tensor2(values)
    return tensor[0] + tensor[1] + tensor[2]
