WIDTH: i32 = 8
HEIGHT: i32 = 8
PIXELS: i32 = WIDTH * HEIGHT

static_assert(PIXELS >= 64)
static_assert(PIXELS <= 64)
static_assert(PIXELS > 63)
static_assert(PIXELS < 65)
static_assert(PIXELS != 63)


def main() -> i32:
    return PIXELS - 22
