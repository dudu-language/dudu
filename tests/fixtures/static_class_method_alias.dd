class Math:
    def add(a: i32, b: i32) -> i32:
        return a + b

    def twice(value: i32) -> i32:
        return class.add(value, value)


def main() -> i32:
    if Math.twice(21) == 42:
        return 42
    return 1
