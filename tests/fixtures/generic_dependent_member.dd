class Encodable:
    value: i32

    def encoded(self: &const[Self]) -> i32:
        return self.value


def encode[T](value: &const[T]) -> i32:
    return value.encoded()


def main() -> i32:
    value = Encodable(42)
    return encode(value)
