class Sample:
    value: i32
    positive: bool


def make_sample(value: i32) -> Sample:
    return Sample(value=value, positive=value > 0)


def main() -> i32:
    sample = make_sample(41)
    if sample.positive:
        return sample.value + 1
    return 0
