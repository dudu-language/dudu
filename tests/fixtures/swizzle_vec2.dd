class Vec2:
    x: i32
    y: i32


def main() -> i32:
    value: Vec2 = Vec2(10, 32)
    swapped: Vec2 = value.yx
    repeated: Vec2 = value.xx
    if swapped.x == 32 and swapped.y == 10 and repeated.y == 10:
        return 42
    return 1
