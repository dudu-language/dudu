from cpp import boost/asio.hpp


def on_post():
    pass


def main() -> i32:
    context = boost.asio.io_context()
    boost.asio.post(context, on_post)
    completed = context.run()
    if completed != 1:
        return 1
    return 0
