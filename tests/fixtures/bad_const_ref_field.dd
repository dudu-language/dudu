class Coin:
    active: bool


def is_active(coin: &const[Coin]) -> bool:
    return coin.missing


def main() -> i32:
    coin = Coin(active=True)
    if is_active(coin):
        return 1
    return 0
