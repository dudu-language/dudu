# Target diagnostic: fixed-arity library hooks reject new-axis indexing unless
# the library explicitly accepts a new_axis index item.

class ScalarOnly:
    @operator("[]")
    def at(self, index: i32) -> i32:
        return index


def main() -> i32:
    value = ScalarOnly()
    return value[None]
